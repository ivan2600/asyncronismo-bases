const XMLhttpRequest = require('xmlhttprequest');
const API = 'https://api.escuelajs.co/api/v1';

function fetchData(urlAPI, callback) {
  let xhttp = newHttpRecuest();

  xhttp.open('GET', urlApi, true);
  xhttp.onready = statecance +
}